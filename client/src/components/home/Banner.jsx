const Banner = () => {
  return (
    <div className="w-full py-2.5 font-medium text-sm text-white/90 text-center bg-linear-to-r from-cc-accent/40 via-cc-bg to-cc-accent/30 border-b border-white/10">
      <p>
        <span className="px-3 py-1 rounded-lg text-white bg-cc-accent mr-2">
          New
        </span>
        ATS checker, analytics & interview prep — now on CareerCraft
      </p>
    </div>
  );
};

export default Banner;
